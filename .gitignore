# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/

# Data & runtime output
# score_history.json is always runtime-only — never commit
data/score_history.json
data/history.json
output/

# pools/ contains seed data — commit it
# (Only core_pool.json and radar_pool.json are seed; score_history.json grows at runtime)
data/pools/score_history.json

# OS
.DS_Store
*.swp
